# Fully isolated runtime.  Do not install these into the system or user site.
# This CPU PyTorch build is intentional: the discovered user-site CUDA 13 build
# is incompatible with the host's CUDA 12.6 driver and is not reused.
numpy==1.26.4
matplotlib==3.8.4
torch==2.5.1
torchvision==0.20.1
ultralytics==8.3.0
opencv-python==4.11.0.86
PyYAML==6.0.2
psutil==6.1.1
scipy==1.15.3
onnx==1.16.2
onnxruntime==1.19.2
